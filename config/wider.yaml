# =====================
# DATA CONFIG (same for fairness)
# =====================
data:
  train_dir: raw_data/FruitinAmazon/train
  val_dir: raw_data/FruitinAmazon/val
  image_size: 128
  batch_size: 4

# =====================
# MODEL CONFIG
# =====================
model:
  name: wider

# =====================
# TRAINING CONFIG (unchanged for fair comparison)
# =====================
training:
  epochs: 20
  learning_rate: 0.001
  patience: 3
  device: cpu

# =====================
# EXPERIMENT CONFIG
# =====================
experiment:
  name: exp_wider